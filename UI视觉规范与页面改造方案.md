# 星辰二手书 UI 视觉规范与页面改造方案

## 第一部分：全局规范

### 1. 视觉定位

面向大学生的校园二手书信息撮合产品。视觉只使用书本、书页、书脊、牛皮纸、书签、油墨印刷、图书馆纸张意象，整体低饱和、复古、简洁、护眼。禁止引入与书籍无关的装饰主题、渐变主色、荧光色和 Emoji 图标。

### 2. 色彩 Token

| Token | 色值 | 用途 |
|---|---:|---|
| 油墨蓝 `--ink-blue` | `#2C425B` | 主按钮、主标题、书脊、功能线框、聚焦态 |
| 牛皮纸 `--kraft` | `#E8D9C3` | 缺省封面、次级底色、书页分层阴影 |
| 书签红 `--bookmark-red` | `#C85A5A` | 收藏、危险操作、折角标签、提示书签 |
| 纸张米白 `--paper` | `#F9F7F2` | 页面、卡片、输入区背景 |
| 正文 `--text` | `#222222` | 正文、关键数值 |
| 辅助 `--muted` | `#666666` | 说明、时间、次级信息 |

只允许以上色值及其透明度。页面不使用纯白、纯黑或新增主色。

### 3. 尺寸、间距与比例

- 基准页面水平留白：`24rpx`。
- 组件内间距：`16rpx`；复杂表单卡可使用 `24rpx` 外壳留白，内部控件仍按 `16rpx` 节奏排列。
- 模块间距：`32rpx`。
- 通用圆角：`12rpx`；弹窗或未来大容器：`20rpx`。
- 最小触控高度：`80rpx`；紧凑管理按钮不得低于 `72rpx`，其所在整行触控区域不得低于 `80rpx`。
- 功能图标：外框 `48rpx × 48rpx`，标准线宽 `2rpx`；搜索图形因圆环描边可使用 `4rpx` 保证小屏清晰度。
- 商品卡外层宽高比固定为 `2:3`。实现使用外层 `height: 0; padding-top: 150%` 与绝对定位内容，不依赖 `aspect-ratio`。
- 商品卡封面固定 `4:5`，占卡片高度 `83.333%`；余下 `16.667%` 展示价格和学校，标题以纸签叠放在封面底部。
- 详情封面和上传缩略图均用 `height: 0; padding-top: 125%` 固定 `4:5`；图片统一 `mode="aspectFill"`，不得拉伸。
- 双列间距：横向 `16rpx`、纵向 `20rpx`。该布局在 `320–430px` 宽设备上保持双列和固定比例。

### 4. 字体与排版

全局字体声明：`"Source Han Serif SC", "Noto Serif CJK SC", "Songti SC", serif`。不打包、不下载外部字体二进制。

- 页面主标题：`42–48rpx`，字重 `700`，行高 `1.28–1.35`。
- 模块标题：`32–36rpx`，字重 `700`。
- 正文：`28rpx`，行高 `1.6` 或 `44–48rpx`。
- 辅助文案：`20–24rpx`，颜色 `#666666`。
- 价格：列表 `30rpx`，详情 `46rpx`，颜色 `#C85A5A`。
- 英文装饰文案改为中文印刷式短语，字距 `6–7rpx`，避免弱语义装饰。

### 5. 图标、标签与分割线

- 图标只用 WXSS 边框、直线、矩形和文字单字印章构成，不使用 Emoji、外部 SVG 或新增二进制素材。
- 搜索：圆环加斜线；添加、删除：两条 `2rpx` 直线；前进：两条边框旋转；书架和书本：矩形书脊组合。
- 成色、分类、状态、封面标签均为折角书签：书签红底、米白字，右侧以透明边框切出折角。
- 分割线固定 `2rpx`，颜色使用油墨蓝 `18%–42%` 透明度。
- 单字印章使用油墨蓝或书签红底，米白字；不得使用随机彩色圆形图标。

### 6. 卡片、表单、按钮与状态组件

#### 商品卡

- 背景 `#F9F7F2`，边框 `2rpx rgba(44,66,91,.28)`，圆角 `12rpx`。
- 书页分层阴影：第一层 `4rpx 6rpx 0 rgba(44,66,91,.16)`；第二层 `9rpx 12rpx 0 rgba(232,217,195,.82)`。
- 缺省封面固定牛皮纸底，包含油墨蓝书脊、首字印刷、三条 `2rpx` 印刷线；不读取随机封面色。

#### 表单

- 背景纸张米白；表单卡边框 `2rpx rgba(44,66,91,.26)`，圆角 `12rpx`，牛皮纸分层阴影 `6rpx 8rpx 0 rgba(232,217,195,.68)`。
- 输入控件使用 `2rpx` 油墨蓝透明下划线，不使用完整圆角输入框。
- 聚焦时下划线转油墨蓝，并以 `8rpx` 书签红内侧色条提示当前书写位置。
- 必填标识使用书签红文字“· 必填”，不使用醒目星号堆叠。

#### 按钮

- 主按钮：油墨蓝底、米白字、`12rpx` 圆角、最小高度 `80rpx`。
- 危险或收藏高亮：书签红；普通收藏使用红色描边，高亮后红色文字与边框。
- 次按钮：透明或牛皮纸 `38%–58%` 背景，油墨蓝文字和 `2rpx` 描边。
- 禁用态只降低透明度至 `48%`，不新增灰色。

#### 空、错、加载

- 空状态：纯 CSS 开本与书架简笔画，标题 `32rpx`，说明 `25rpx`，操作按钮为牛皮纸次按钮。
- 错误状态：复用空状态组件，保留明确重试按钮。
- 全局加载：纯 CSS 开合书本，周期 `960ms`、交替循环；骨架只做透明度 `48%–84%` 脉冲。
- 所有加载动画只改变 `transform` 和 `opacity`；静态骨架保持商品卡 `2:3`。

### 7. 动效

- 页面与列表进入：`opacity: 0→1`、`translateX(18rpx→0)`，`240ms ease-out`。
- 商品卡按压：下沉 `6rpx`，同步缩短阴影，`120ms ease-out`。
- 普通列表按压：下沉 `4rpx`，`120ms ease-out`。
- 收藏成功：`18rpx × 36rpx` 书签向上漂浮 `72rpx`，透明度 `1→0`，`480ms ease-out`。
- Loading 开合书本：左右书页旋转，`960ms ease-in-out infinite alternate`。
- 禁止持续改变布局属性、背景位置、模糊半径或大面积阴影。

## 第二部分：逐页面规范

### 1. 首页

- 背景：纸张米白。
- 标题：油墨蓝书封式 Hero，左侧书签红书脊，中文印刷小标题与 `46rpx` 主标题。
- 卡片：双列固定 `2:3` 商品卡，封面 `4:5`，缺省牛皮纸封面。
- 标签：分类选中态为油墨蓝页签并带书签红页码；成色为折角书签。
- 按钮：搜索入口为米白纸签；空态“去发布”为次按钮。
- 分割线：模块标题下 `2rpx` 油墨线，加载尾部两侧为淡油墨线。
- 图标：CSS 搜索镜、书本叠放。
- 动效：页面翻页进入 `240ms`；卡片按压下沉 `120ms`。

### 2. 列表／搜索

- 背景：纸张米白。
- 标题：导航标题“搜索好书”；结果数量作为 `23rpx` 辅助标题。
- 卡片：结果区复用双列 `2:3` 商品卡。
- 标签：搜索历史为牛皮纸折角纸签。
- 按钮：取消为透明文字按钮；错误态提供重试次按钮。
- 分割线：搜索输入底部 `2rpx` 油墨线，历史与结果标题使用印刷线。
- 图标：CSS 搜索镜；输入右侧固定小书签提示焦点。
- 动效：结果区翻页进入 `240ms`；加载使用开合书本。

### 3. 书籍详情

- 背景：纸张米白。
- 标题：书名 `43rpx`，作者和时间 `23rpx`；价格 `46rpx` 书签红。
- 卡片：详情封面固定 `4:5`，双层书页阴影；信息摘要为牛皮纸三栏。
- 标签：封面成色、档案分类均为折角书签。
- 按钮：主操作“我想要”为油墨蓝；收藏高亮为书签红；分享为牛皮纸次按钮；举报为红色描边。
- 分割线：详情模块使用 `2rpx` 淡油墨线。
- 图标：卖家头像为单字印章；安全提示为“阅”字书章。
- 动效：收藏成功触发 `480ms` 漂浮书签；底部按钮最小高度 `80rpx`。

### 4. 发布

- 背景：纸张米白。
- 标题：油墨蓝登记簿 Hero，书签红书脊与牛皮纸“传”字书本。
- 卡片：上传图固定 `4:5`；表单按“书籍档案”“交接方式”分成纸张卡。
- 标签：首图“封面”、成色和分类均使用折角书签。
- 按钮：确认发布为油墨蓝主按钮；清空草稿为书签红描边；扫码为牛皮纸次按钮。
- 分割线：字段统一 `2rpx` 油墨下划线。
- 图标：添加、删除用 `48rpx` CSS 线框；Picker 使用小书签，不使用箭头字符。
- 动效：页面进入 `240ms`；图片与字段不使用持续动画；提交使用原生按钮 Loading。

### 5. 我的书架

- 背景：纸张米白。
- 标题：油墨蓝藏书票卡，书签红书脊；头像改为 `4:5` 小书封。
- 卡片：统计区为牛皮纸三栏；菜单为米白书架管理卡。
- 标签：菜单图标为油墨蓝、牛皮纸或书签红单字书脊。
- 按钮：编辑资料为透明米白描边按钮。
- 分割线：统计和菜单使用 `2rpx` 淡油墨线。
- 图标：纯 CSS 叠书、书脊、前进线框。
- 动效：首次加载使用开合书本；页面进入 `240ms`。

### 6. 我的发布／收藏

- 背景：纸张米白。
- 标题：`36rpx` 页面标题加 `2rpx` 油墨延伸线。
- 卡片：管理场景使用横向书目卡，封面仍固定 `4:5`，卡体为米白纸张与牛皮纸分层阴影。
- 标签：在售、已售、下架状态使用折角书签。
- 按钮：标记售出为牛皮纸次按钮；下架、取消收藏为书签红描边。
- 分割线：卡片内部不堆叠多余线条，列表卡间距 `16rpx`。
- 图标：缺省封面含油墨书脊和书名首字。
- 动效：管理卡按压下沉 `4rpx/120ms`；加载使用开合书本。

### 7. 浏览足迹

- 背景：纸张米白。
- 标题：`36rpx` 标题加油墨延伸线。
- 卡片：复用双列固定 `2:3` 商品卡。
- 标签：复用成色折角书签。
- 按钮：空态提供“去逛首页”次按钮。
- 分割线与图标：沿用全局书架空态和印刷线。
- 动效：列表翻页进入 `240ms`，卡片按压 `120ms`。

### 8. 资料

- 背景：纸张米白。
- 标题：油墨蓝校园藏书票，配牛皮纸“我”字书本。
- 卡片：资料表单为米白纸张卡，头像为 `4:5` 小书封。
- 标签：学校选择使用小书签。
- 按钮：保存为油墨蓝主按钮；注销为书签红描边危险按钮。
- 分割线：输入项 `2rpx` 油墨下划线。
- 图标：不使用人物图标，仅保留头像书封和书签。
- 动效：加载、错误均有独立可恢复状态；页面进入 `240ms`。

### 9. 关于

- 背景：纸张米白。
- 标题：油墨蓝书封，书签红书脊和“星辰”藏书章。
- 卡片：内容不套多层卡片，按三章排版。
- 标签：章节号为书签红竖排书签。
- 按钮：复制安全提示为牛皮纸次按钮。
- 分割线：章节间 `2rpx` 油墨线。
- 图标：只使用藏书章与书脊。
- 动效：页面翻页进入 `240ms`。

### 10. 反馈

- 背景：纸张米白。
- 标题：油墨蓝便笺封面，右侧书签红“言”字书签。
- 卡片：反馈正文为米白信纸卡。
- 标签：字段使用文字标签，不增加装饰色。
- 按钮：提交反馈为油墨蓝主按钮。
- 分割线：输入和文本域均为 `2rpx` 油墨下划线。
- 图标：书签与印刷线。
- 动效：页面进入 `240ms`；提交使用原生 Loading。

### 11. 举报

- 背景：纸张米白。
- 标题：牛皮纸风险提示，左侧书签红边线与“核”字书本。
- 卡片：举报表单为米白纸张卡。
- 标签：问题类型使用小书签选择标记。
- 按钮：提交举报为书签红危险按钮。
- 分割线：字段使用 `2rpx` 油墨下划线。
- 图标：书本核查章，不使用警告 Emoji。
- 动效：页面进入 `240ms`；提交使用原生 Loading。

### 12. 未来购物车

当前不启用购物车，不创建页面。当前产品语义映射为“我的收藏”，只保存感兴趣书籍，不暗示结算。

若未来产品边界正式启用：背景 `#F9F7F2`；条目封面 `120rpx × 150rpx`（`4:5`）；清单卡圆角 `12rpx`、间距 `16rpx`；选择标记为 `48rpx` 油墨线框；价格用书签红；主按钮高度 `88rpx`、油墨蓝；不得在未接支付时显示“结算”。

### 13. 未来订单

当前不启用订单，不创建页面。当前产品语义映射为“购买意向”，仅记录“我想要”和受控联系动作，不构成订单、担保或支付凭证。

若未来产品边界正式启用：背景 `#F9F7F2`；订单卡圆角 `12rpx`，头部与内容以 `2rpx` 油墨线分隔；状态使用书签红折角标签；封面 `96rpx × 120rpx`（`4:5`）；主要履约按钮油墨蓝、危险取消按钮书签红；时间线节点为 `16rpx` 油墨圆点与 `2rpx` 竖线。

### 14. 未来消息聊天

当前不启用站内聊天，不创建页面。当前产品语义映射为“受控联系方式”：用户明确确认购买意向后，平台受控展示微信号或手机号并记录审计。

若未来产品边界正式启用：背景 `#F9F7F2`；对方消息使用牛皮纸 `58%` 背景，本方消息使用油墨蓝背景与米白字；气泡圆角 `12rpx`，最大宽度 `540rpx`；输入区顶部 `2rpx` 油墨线；发送按钮 `80rpx` 高、油墨蓝；书籍上下文卡封面 `72rpx × 90rpx`（`4:5`）；不使用彩色头像环或 Emoji 功能图标。

## 第三部分：三级改造

| 级别 | 范围 | 交付标准 |
|---|---|---|
| 快速简易 | 换色、字体、基础圆角和按钮 | 统一六色 Token，修正明显旧色，不调整结构与状态组件 |
| 中度升级 | 快速简易 + 通用组件与主要页面结构 | 重构商品卡、空态、骨架、表单、首页、详情、发布、我的；加入基础比例和动效 |
| 完整改版 | 中度升级 + 全页面、全状态、交互细节与规范文档 | 覆盖全部现有页面；固定卡片和封面比例；统一加载、错误、空态；落实书签标签、书页阴影、油墨表单、CSS 图标、翻页与收藏动效；完成静态校验 |

**本次实际落地：完整改版。** 已覆盖 `app.wxss`、`app.json`、三个通用组件及全部现有主包／分包页面；保持原生微信小程序、CommonJS、现有业务服务调用和“不支付、不聊天、线下交易”的产品边界。
