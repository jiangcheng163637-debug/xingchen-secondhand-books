# 星辰二手书：从零上线并可被微信搜索（傻瓜式教程）

> 目标：把本地工程部署为微信正式版，让朋友在微信顶部搜索框中输入小程序名称后能够进入。
>
> 官方发布链路：**注册/完善资料 → 小程序备案 → 部署正式云环境 → 真机测试 → 上传代码 → 提交审核 → 审核通过 → 手动发布 → 等待搜索收录**。

---

## 一、先看你当前处于什么状态

当前工程已经具备：

- 正式格式的 AppID 已写入 `project.config.json`；
- 小程序页面、分包和四个云函数代码已经齐全；
- `sitemap.json` 已允许微信索引全部页面；
- 账号注销、举报、文本安全和联系方式隔离已经实现。

但当前仍然是：

```js
// miniprogram/config/env.js
module.exports = {
  enableCloud: false,
  cloudEnvId: '',
  pageSize: 6
};
```

也就是说，现在运行的是 **Mock 本地演示模式**。每台手机的数据互不相通，不能作为正式线上服务。上线前必须创建云环境、部署数据库与云函数，然后把 `enableCloud` 改为 `true`。

### 当前最重要的上线阻断��

书籍文字已接入文本安全检查，但用户上传图片目前还没有完整的异步媒体安全检测和复审流程。

正式提交审核前必须至少完成一种方案：

1. **推荐方案：**接入微信当前可用的图片/媒体内容安全接口，检测通过后再公开书籍；
2. **临时方案：**新书先进入“待审核”，管理员在云控制台确认图片无问题后再改为“已发布”；
3. **最保守方案：**首版暂时关闭用户上传图片功能。

不要在图片未经审核、上传后立即公开的状态下直接提审。UGC 内容治理是常见审核重点。

---

# 第一阶段：注册并确认小程序账号

## 第1步：打开微信公众平台

浏览器打开：

```text
https://mp.weixin.qq.com/
```

注意：公众号和小程序都在这个网站登录，必须登录“小程序”账号，不要误进公众号后台。

## 第2步：确认工程里的 AppID 属于你的账号

登录后台，寻找：

```text
开发管理 → 开发设置 → 开发者 ID → AppID
```

打开本地工程根目录的：

```text
project.config.json
```

确认���中 `appid` 与后台显示的 AppID 完全一致。

如果不一致：

1. 复制后台 AppID；
2. 替换 `project.config.json` 中的 `appid`；
3. 保存；
4. 关闭微信开发者工具中的项目；
5. 重新导入工程。

### 验收标准

微信开发者工具打开项目后，不出现“当前登录用户不是该 AppID 的开发者”之类的提示。

---

# 第二阶段：完善名称、头像、简介与类目

## 第3步：填写小程序基本信息

在微信公众平台首页或：

```text
设置 → 基本设置 / 账号设置
```

完善以下信息。

### 建议名称

```text
星辰二手书
```

前提是名称可用，且不会侵犯学校、企业或他人的商标权益。

如果已被占用，可考虑：

```text
星辰校园二手书
星辰校园书市
星辰旧书流转
```

名称确定后不要频繁修改。朋友以后主要通过准确名称搜索。

### 建议简介（可直接复制）

```text
校园二手教材与课外书信息发布、搜索、收藏和联系撮合工具。平台不提供在线支付或交易担保，建议校内当面验书交易。
```

简介必须与实际功能一致，不要写“在线交易”“担保交易”“微信支付”，因为当前产品没有这些能力。

### 头像建议

- 正方形高清图片；
- 油墨蓝背景、牛皮纸书本图形；
- 图形与“星辰二手书”名称一致；
- 不使用微信官方图标；
- 不使用模糊截图或大量小字。

### 服务类目怎么选

类目会因主体类型和微信当时规则不同而变化，不能死记某个旧教程的名称。

操作原则：

1. 在后台点击“服务类目 → 添加”；
2. 选择与“二手书信息发布、查询和联系撮合”最接近的当前可选类目；
3. 不要为了通过审核错选“教育培训”“社交论坛”或“电商平台”；
4. 如果个人主体没有匹配类目，不要硬选，优先考虑使用个体工商户或企业主体；
5. 提交前查看该类目是否需要营业执照或其他资质。

**特别提醒：**本小程序允许用户发布书籍信息，属于用户生成内容场景。审核人员会重点检查举报、内容审核、违规下架和账号删除能力。

### 验收标准

后台“名称、头像、简介、服务类目”均显示已设置，名称和简介与实际功能一致。

---

# 第三阶段：完成小程序备案

## 第4步：进入备案入口

中国境内提供互联网信息服务的小程序需要完成备案。

后台入口通常为：

```text
首页 → 去备案
```

或：

```text
设置 → 小程序备案 → 去备案
```

备案免费。

## 第5步：准备备案材料

### 个人主体一般准备

- 本人有效身份证件；
- 本人常用手机号；
- 实际通讯地址；
- 管理员本人完成人脸核验。

### 企业/个体工商户主体一般准备

- 营业执照等主体证件；
- 法定代表人/负责人证件；
- 小程序负责人证件与手机号；
- 必要时提供授权书或其他补充材料。

## 第6步：填写备案信息

按后台页面逐项如实填写：

1. 主体信息；
2. 主体负责人信息；
3. 小程序信息；
4. 小程序负责人信息；
5. 完成人脸核验；
6. 阅读并确认互联网信息服务承诺书；
7. 提交平台初审。

### 小程序服务内容建议描述

```text
为高校学生提供二手教材和课外书的信息发布、搜索、收藏及联系方式撮合服务，不提供在线支付、交易担保或物流服务。
```

## 第7步：完成工信部短信核验

平台初审通过后，留意 **12381** 开头的短信。

收到短信后：

1. 在24小时内打开：`https://beian.miit.gov.cn/`；
2. 进入短信核验；
3. 填写验证码、手机号及证件号码后6位；
4. 完成核验。

超过24小时未核验，备案可能被退回，需要重新提交。

## 第8步：等待管局审核

一般流程：

```text
微信平台初审 → 工信部短信核验 → 省通信管理局审核 → 备案成功
```

官方说明中，平台初审通常为1—2个工作日，管局审核通常为1—20个工作日，以实际结果为准，不能加急。

### 验收标准

后台“设置 → 小程序备案”显示备案成功，并取得备案号。

---

# 第四阶段：创建正式云环境

## 第9步：用微信开发者工具打开项目

1. 下载最新版稳定版微信开发者工具；
2. 使用小程序管理员或开发者微信扫码登录；
3. 导入包含 `project.config.json` 的目录：

```text
星辰二手书小程序/
```

4. 点击“编译”，确认项目可以运行。

## 第10步：开通云开发

在开发者工具顶部点击：

```text
云开发
```

按提示开通，并建议创建两个环境：

```text
开发环境：xingchen-book-dev
生产环境：xingchen-book-prod
```

第一次上线也可以只建一个环境，但长期使用建议开发、生产分开。

创建后会得到类似下面的环境 ID：

```text
cloud1-xxxxxxxxxxxx
```

代码中填写的是**环境 ID**，不是你自己起的环境名称。

---

# 第五阶段：创建数据库

## 第11步：创建9个集合

进入：

```text
云开发控制台 → 数据库
```

逐个创建：

```text
users
books
seller_contacts
favorites
history
intentions
contact_views
reports
feedback
```

## 第12步：设置数据库权限

每个集合均设置为：

```text
仅管理端/云函数可读写
```

不要设置为“所有用户可读写”。尤其是 `seller_contacts`，里面存放加密联系方式，必须禁止客户端直接访问。

## 第13步：创建索引

进入每个集合的“索引管理”，按 `cloudfunctions/CLOUD_SETUP.md` 创建索引。

最重要的 `books` 索引包括：

```text
status + deletedAt + publishedAt(降序) + _id(降序)
status + deletedAt + schoolId + campusId + publishedAt(降序) + _id(降序)
status + deletedAt + schoolId + campusId + category + publishedAt(降序) + _id(降序)
sellerOpenId + deletedAt + createdAt(降序) + _id(降序)
```

如果运行时提示“缺少索引”，按云函数日志中的字段和方向补建，等待索引构建完成后再测试。

### 验收标准

云控制台能看到9个集合，且没有集合开放为所有用户可写。

---

# 第六阶段：配置联系方式加密密钥

## 第14步：生成随机密钥

macOS 打开“终端”，执行：

```bash
openssl rand -base64 32
```

复制生成的一串字符。

## 第15步：给两个云函数配置同一密钥

在云开发控制台中分别给：

```text
book
engagement
```

添加环境变量：

```text
名称：CONTACT_ENCRYPTION_KEY
值：刚才生成的同一串密钥
```

两个函数必须完全相同。不要把密钥写进代码、截图或公开文档。

### 验收标准

`book` 与 `engagement` 都能看到 `CONTACT_ENCRYPTION_KEY`，且值一致。

---

# 第七阶段：部署四个云函数

## 第16步：依次部署

在微信开发者工具左侧，依次右键：

```text
cloudfunctions/user
cloudfunctions/book
cloudfunctions/engagement
cloudfunctions/moderation
```

每一个都选择：

```text
上传并部署：云端安装依赖
```

不要选择“只上传代码，不安装依赖”。

建议顺序：

```text
user → book → engagement → moderation
```

## 第17步：检查函数权限

`user`、`book`、`moderation` 目录已经包含文本安全开放接口权限配置。部署后进入云函数详情，确认部署成功，并查看日志中是否有依赖或权限错误。

### 验收标准

云开发控制台“云函数”页面能看到4个函数，状态正常，没有部署失败提示。

---

# 第八阶段：把小程序切换到正式云模式

## 第18步：修改环境配置

打开：

```text
miniprogram/config/env.js
```

修改为：

```js
module.exports = {
  enableCloud: true,
  cloudEnvId: '你的生产环境ID',
  pageSize: 6
};
```

保存。

## 第19步：清缓存并重新编译

在微信开发者工具中：

1. 清除全部缓存；
2. 重新编译；
3. 打开调试器 Console；
4. 打开云函数日志。

这一步很重要。否则你可能把之前的 Mock 本地数据误认为云端数据。

### 验收标准

打开“我的”后，云数据库 `users` 集合出现当前用户记录。

---

# 第九阶段：上线前完整测试

## 第20步：用账号A测试发布

1. 打开“我的 → 编辑资料”；
2. 设置头像、昵称、学校、校区；
3. 发布一本测试书；
4. 确认图片进入云存储；
5. 确认 `books` 出现书籍记录；
6. 确认 `seller_contacts` 出现联系方式记录；
7. 检查 `books` 中没有联系方式明文。

## 第21步：用账号B测试联系

把账号B添加为体验成员，或使用预览/体验版测试：

1. 账号B进入小程序；
2. 找到账号A发布的书；
3. 收藏；
4. 点击“我想要”；
5. 查看并复制联系方式；
6. 检查 `contact_views` 出现审计记录；
7. 确认账号B不能下架或编辑账号A的书。

## 第22步：测试这些异常情况

- 不填写书名；
- 不上传图片；
- 输入错误手机号；
- 拒绝相册或摄像头权限；
- 重复点击发布；
- 断网后重试；
- 举报书籍；
- 注销账号；
- 卖家把书标记为已售或下架。

## 第23步：iOS和Android真机都测试

开发者工具模拟器不能替代真机。至少测试：

- 一台iPhone；
- 一台Android；
- 微信头像昵称；
- 相册/拍照；
- ISBN扫码；
- 分享卡片；
- 复制联系方式；
- 弱网环境。

### 验收标准

两个微信账号能完成“发布 → 浏览 → 收藏 → 查看联系方式 → 售出/下架”完整流程，没有明显报错。

---

# 第十阶段：填写隐私保护指引

## 第24步：进入隐私设置

入口可能为：

```text
设置 → 功能设置 / 服务内容声明 → 用户隐私保护指引
```

也可能在提交审核页面底部要求完善，以后台实际显示为准。

## 第25步：按实际功能声明

本项目需要说明的内容至少包括：

| 信息类型 | 用途说明 |
|---|---|
| 用户选择的头像 | 在个人资料和卖家信息中展示身份 |
| 用户填写的昵称 | 展示发布者身份，便于校园书籍撮合 |
| 用户选择的图片 | 发布二手书封面和书况照片 |
| 用户填写的手机号或微信号 | 用户确认购买意向后，受控展示给买家 |
| 用户填写的书籍、反馈、举报内容 | 完成信息发布、问题反馈和违规治理 |
| 必要操作记录 | 防滥用、安全审计和处理举报 |

必须明确：

- 联系方式不会直接展示在公开列表；
- 用户确认联系后才受控展示；
- 用户可以在“我的 → 编辑资料”注销账号；
- 注销后个人资料会清除，在售书籍会下架。

### 验收标准

提交审核页面不再提示隐私指引缺失，声明内容与代码实际行为一致。

---

# 第十一阶段：上传体验版

## 第26步：上传代码

微信开发者工具顶部点击：

```text
上传
```

建议填写：

```text
版本号：1.0.0
项目备注：星辰二手书首个正式候选版本，包含书籍发布、搜索、收藏、联系、举报和个人中心。
```

上传成功不等于上线，此时只是“开发版本”。

## 第27步：设置体验版

登录微信公众平台：

```text
管理 / 开发管理 → 版本管理 → 开发版本
```

找到 `1.0.0`，设置为体验版。

把朋友添加为体验成员，让他们扫码测试。体验成员能用，不代表普通微信用户能搜索到。

### 验收标准

体验成员扫码后可以完整使用云端版本，且彼此能看到同一批书籍数据。

---

# 第十二阶段：提交微信审核

## 第28步：点击“提交审核”

后台路径：

```text
版本管理 → 开发版本 → 1.0.0 → 提交审核
```

## 第29步：填写审核功能页面

建议配置不超过5组核心页面：

1. 首页：`pages/index/index`
2. 发布：`pages/publish/publish`
3. 我的书架：`pages/mine/mine`
4. 书籍详情：可通过首页进入
5. 举报/反馈：可通过详情或我的进入

类目、标签必须和后台已选服务类目一致。

## 第30步：审核说明（可直接复制）

```text
本小程序为校园二手教材与课外书信息撮合工具。用户可选择学校和校区，发布书名、价格、书况、图片及联系方式；其他用户可搜索、收藏，并在明确确认联系后受控查看卖家联系方式。平台不提供在线支付、交易担保、物流或站内聊天，建议双方在校内公共区域当面验书后线下交易。

审核路径：
1. 首页可浏览和搜索书籍；
2. 点击底部“发布”可发布闲置书；
3. 进入书籍详情可收藏、分享、举报及确认联系卖家；
4. “我的”可查看发布、收藏和浏览记录；
5. “我的→编辑资料”可修改资料或注销账号；
6. “我的→意见反馈”可提交反馈。

小程序使用微信 OpenID 自动初始化用户，不需要审核员输入账号密码。若需体验双方联系流程，可使用两个微信账号分别发布和查看。
```

## 第31步：上传截图和补充材料

建议准备：

- 首页截图；
- 发布页截图；
- 详情页截图；
- 举报入口截图；
- 注销入口截图；
- 隐私处理和内容安全说明。

如果审核页面要求测试账号，而项目没有密码登录体系，可在说明中写明“使用微信身份自动初始化，无独立账号密码”。

### 验收标准

版本状态变成“审核中”。

---

# 第十三阶段：处理审核结果

## 审核通过

管理员微信会收到通知。注意：**审核通过后还没有自动上线**，你还要手动发布。

## 审核被拒

不要盲目再次提交。打开驳回原因，按类型处理：

### 类目不符

- 改为与实际服务一致的类目；
- 如果个人主体没有适配类目，考虑更换主体类型；
- 不要通过隐藏功能绕过类目审核。

### 隐私说明不完整

- 按平台识别出的信息类型逐项说明用途；
- 确保代码行为、页面文案和隐私指引一致。

### UGC治理不足

- 展示举报入口；
- 完善文本和图片安全；
- 增加待审核/下架流程；
- 提供管理员处理机制。

### 功能打不开或没有数据

- 检查生产环境 ID；
- 检查四个云函数；
- 检查集合权限和索引；
- 准备几本合规的线上演示书籍。

---

# 第十四阶段：正式发布

## 第32步：手动发布

审核通过后进入：

```text
版本管理 → 审核版本
```

点击：

```text
发布
```

第一次上线建议选择：

```text
全量发布
```

确认后才是真正的线上正式版。

### 验收标准

“版本管理 → 线上版本”中能看到 `1.0.0`。

---

# 第十五阶段：让朋友在微信里搜到

## 第33步：确认允许被搜索

后台不同版本的菜单名称可能不同，在“设置、功能设置、基本设置”中查找与以下含义一致的选项：

```text
允许被搜索 / 搜索开放 / 隐私设置中的搜索可见性
```

确保没有主动关闭搜索入口。

## 第34步：确认 sitemap 已允许索引

工程当前已有：

```json
{
  "desc": "星辰二手书页面索引规则",
  "rules": [
    { "action": "allow", "page": "*" }
  ]
}
```

这表示页面允许被微信索引，无需修改。

## 第35步：朋友如何搜索

让朋友在微信顶部搜索框输入准确名称：

```text
星辰二手书
```

然后切换或查看“小程序”结果。

新小程序发布后，搜索收录可能不是瞬间完成。如果刚发布搜不到：

1. 先确认线上版本确实已发布；
2. 用管理员微信打开小程序并正常使用一次；
3. 检查搜索可见性是否关闭；
4. 检查名称是否与备案、后台信息一致；
5. 等待平台完成索引后再搜索；
6. 先把正式小程序码发给朋友使用。

## 第36步：下载正式小程序码

发布后，在后台寻找：

```text
工具 / 小程序码 / 推广 / 基本设置中的小程序码
```

下载正式小程序码，可发到：

- 微信好友；
- 班级群；
- 校园社群；
- 海报；
- 书籍交换活动现场。

不要用开发工具生成的预览二维码当正式入口，预览二维码只对开发者或体验成员有效。

---

# 第十六阶段：发布后的维护

## 每天或每周检查

进入微信公众平台和云开发控制台查看：

- 用户访问量；
- 云函数错误日志；
- 云数据库调用量；
- 云存储容量；
- 举报和反馈；
- 异常联系方式查看；
- 内容安全失败记录。

## 后续更新怎么做

每次修改代码后重复：

```text
本地测试 → 真机测试 → 上传新版本 → 设置体验版 → 提交审核 → 审核通过 → 发布
```

版本号可按：

```text
1.0.1：修复问题
1.1.0：增加功能
2.0.0：重大改版
```

---

# 最终傻瓜式核对清单

## 账号与备案

- [ ] AppID 与后台一致
- [ ] 名称、头像、简介已设置
- [ ] 服务类目与实际功能一致
- [ ] 小程序备案已成功

## 云端

- [ ] 已创建生产云环境
- [ ] 已创建9个数据库集合
- [ ] 集合仅允许云函数/管理端访问
- [ ] 索引已创建
- [ ] `book` 和 `engagement` 使用同一加密密钥
- [ ] 四个云函数部署成功
- [ ] `enableCloud` 已改为 `true`
- [ ] `cloudEnvId` 是生产环境 ID

## 合规与测试

- [ ] 文本内容安全可用
- [ ] 图片审核流程已落地
- [ ] 举报入口可用
- [ ] 注销账号可用
- [ ] 隐私保护指引已填写
- [ ] 双账号测试通过
- [ ] iOS 与 Android 真机通过

## 审核与发布

- [ ] 已上传 `1.0.0`
- [ ] 已设置体验版并验收
- [ ] 已提交审核
- [ ] 审核已通过
- [ ] 已手动点击“发布”
- [ ] 线上版本可打开
- [ ] 搜索可见性没有关闭
- [ ] 正式小程序码可扫码进入
- [ ] 朋友可以按准确名称搜索

---

# 官方参考

- 小程序协同工作与发布：`https://developers.weixin.qq.com/miniprogram/dev/framework/quickstart/release.html`
- 小程序产品定位、信息完善与审核：`https://developers.weixin.qq.com/miniprogram/introduction/index.html`
- 小程序备案操作指引：`https://developers.weixin.qq.com/miniprogram/product/record/guidelines.html`
- 常见审核拒绝情形：`https://developers.weixin.qq.com/miniprogram/product/reject`
- 微信公众平台：`https://mp.weixin.qq.com/`

> 微信后台菜单和类目会更新。如果页面名称与本文略有不同，以后台当前显示为准，但整体顺序不变：备案、云端、上传、审核、发布、搜索。
