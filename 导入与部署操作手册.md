# 星辰二手书｜导入与部署操作手册

> 适用工程：`星辰二手书小程序/`
>
> 推荐顺序：先导入并运行 Mock → 再换正式 AppID → 开通云开发 → 配置数据库和密钥 → 部署云函数 → 切换 Cloud 模式 → 双账号真机测试 → 上传提审。

## 一、准备工作

### 1. 需要准备的账号与软件

1. 一个已注册的小程序账号。
2. 登录小程序后台的管理员或已添加为开发者的微信号。
3. 最新稳定版微信开发者工具。
4. 正式部署时需要从小程序后台取得 AppID。

小程序后台地址：`https://mp.weixin.qq.com/`

在后台的“开发管理 / 开发设置”中查看 AppID。不同后台版本的菜单名称可能略有变化，以页面实际显示为准。

### 2. 工程包

使用交付文件：

```text
星辰二手书小程序.zip
```

解压后，应看到：

```text
星辰二手书小程序/
├── project.config.json
├── README.md
├── miniprogram/
└── cloudfunctions/
```

**导入时一定选择包含 `project.config.json` 的这一层目录。**不要选择 ZIP 文件，也不要只选择 `miniprogram/`。

---

## 二、第一阶段：以 Mock 模式导入并运行

这一阶段不使用真实云环境，目的是先确认界面和本地业务链路正常。

### 1. 导入项目

1. 打开微信开发者工具并登录。
2. 在欢迎页点击“导入项目”。如果已进入其他项目，可从菜单中选择“项目 / 导入项目”。
3. 项目目录选择解压后的：

```text
星辰二手书小程序/
```

4. 工程当前已配置正式格式的 AppID；请确认它与微信公众平台中属于你的 AppID 完全一致。如不一致，先修改 `project.config.json`。
5. 项目名称填写“星辰二手书”。
6. 后端服务暂不需要选择，直接导入。

导入成功后，开发者工具应自动识别：

```text
miniprogramRoot: miniprogram/
cloudfunctionRoot: cloudfunctions/
```

### 2. 确认 Mock 开关

打开：

```text
miniprogram/config/env.js
```

保持：

```js
module.exports = {
  enableCloud: false,
  cloudEnvId: '',
  pageSize: 6
};
```

此时所有服务调用都会进入本地 Mock 仓库，不会调用云函数。

### 3. 编译与基础检查

点击开发者工具顶部“编译”。首次编译后，按以下顺序测试：

1. 首页能看到 12 本演示书籍。
2. 点击分类后书单会刷新。
3. 搜索“高等数学”或“Python”能得到结果。
4. 打开书籍详情，收藏后状态变化。
5. 点击“我想要”，确认后可看到测试联系方式。
6. 发布一本书，返回首页后能看到新书。
7. “我的”中能看到发布数、收藏数和浏览记录。
8. 在“我的发布”中可标记售出或下架。
9. 反馈和举报页面可提交。

Mock 数据保存在开发者工具本地缓存中。需要重置时，可在开发者工具中清除缓存后重新编译。

### 4. Mock 阶段常见问题

#### 导入后提示找不到 `app.json`

选择错了目录。重新导入包含 `project.config.json` 的 `星辰二手书小程序/`，不要选择外层解压目录或 `miniprogram/`。

#### 页面没有演示数据

检查 `miniprogram/config/env.js` 中是否仍为：

```js
enableCloud: false
```

#### 修改代码但页面没有变化

点击“编译”，必要时执行“清缓存并编译”。

---

## 三、第二阶段：绑定正式 AppID

### 1. 获取 AppID

登录小程序后台，在“开发管理 / 开发设置”中复制 AppID。AppID 通常以 `wx` 开头。

### 2. 修改工程 AppID

打开根目录：

```text
project.config.json
```

将：

```json
"appid": "touristappid"
```

替换为：

```json
"appid": "你的正式AppID"
```

保存后，如果开发者工具没有自动刷新，关闭项目并重新导入。

### 3. 权限检查

当前登录开发者工具的微信号必须是：

- 小程序管理员；或
- 已在小程序后台添加的项目成员/开发者。

否则可能无法预览、上传代码或开通云开发。

---

## 四、第三阶段：开通微信云开发

### 1. 创建云环境

1. 在微信开发者工具中打开项目。
2. 点击工具栏中的“云开发”。
3. 根据提示开通云开发。
4. 先创建一个开发环境，例如：

```text
xingchen-book-dev
```

5. 记录系统生成的环境 ID，例如：

```text
cloud1-xxxxxxxx
```

环境名称和环境 ID 不是一回事，代码中需要填写的是**环境 ID**。

如果准备正式上线，建议再创建独立生产环境，例如：

```text
xingchen-book-prod
```

开发环境用于联调，生产环境只放正式数据。不要让测试书籍混入生产环境。

### 2. 让开发者工具识别云函数目录

开通环境后，左侧 `cloudfunctions/` 目录应显示云函数标识。若没有：

1. 确认 `project.config.json` 中存在：

```json
"cloudfunctionRoot": "cloudfunctions/"
```

2. 在开发者工具中选择当前云环境。
3. 重新编译或重新打开项目。

---

## 五、第四阶段：创建云数据库集合

进入云开发控制台的“数据库”，依次创建：

```text
users
books
seller_contacts
favorites
history
intentions
contact_views
reports
feedback
```

### 1. 权限原则

所有集合建议设置为：

```text
仅管理端/云函数可读写
```

或使用等价的自定义安全规则，拒绝小程序客户端直接读写。

本工程页面只通过云函数访问数据，不需要把集合开放给所有用户。如果误设为“所有用户可读写”，联系方式和用户数据会暴露，不能上线。

### 2. 创建索引

在各集合的“索引管理”中创建下列索引。字段顺序很重要：

#### users

```text
_openid：升序
```

#### books：公开书单

```text
status：升序
deletedAt：升序
publishedAt：降序
_id：降序
```

#### books：我的发布

```text
sellerOpenId：升序
deletedAt：升序
createdAt：降序
_id：降序
```

#### books：搜索

```text
status：升序
deletedAt：升序
searchKeywords：升序
publishedAt：降序
_id：降序
```

#### favorites

```text
userOpenId：升序
createdAt：降序
_id：降序
```

#### history

```text
userOpenId：升序
lastViewedAt：降序
_id：降序
```

#### intentions

```text
buyerOpenId：升序
createdAt：降序
```

#### contact_views

```text
viewerOpenId：升序
createdAt：降序
```

#### reports

```text
reporterOpenId：升序
targetType：升序
targetId：升序
status：升序
```

#### feedback

```text
userOpenId：升序
createdAt：降序
```

云数据库可能在第一次查询时提示还需要某个索引。按错误详情给出的字段顺序补建即可，索引创建完成前对应查询会失败。

---

## 六、第五阶段：配置联系方式加密密钥

`book` 云函数负责加密联系方式，`engagement` 云函数负责解密，因此二者必须使用**完全相同**的密钥。

### 1. 生成密钥

可在 macOS 终端执行：

```bash
openssl rand -base64 32
```

复制输出结果，不要带多余空格或换行。

### 2. 配置环境变量

进入云开发控制台，分别打开：

```text
book
engagement
```

在函数配置的“环境变量”中都添加：

```text
变量名：CONTACT_ENCRYPTION_KEY
变量值：刚才生成的同一串密钥
```

请再次确认两个函数的值完全一致。

**不要把密钥写入 `env.js`、README、代码仓库或聊天截图。**如果上线后直接更换密钥，旧联系方式将无法解密，需要先做数据迁移。

---

## 七、第六阶段：部署四个云函数

建议部署顺序：

```text
user → book → engagement → moderation
```

### 1. 上传函数

在微信开发者工具左侧依次右键：

```text
cloudfunctions/user
cloudfunctions/book
cloudfunctions/engagement
cloudfunctions/moderation
```

每个目录都选择：

```text
上传并部署：云端安装依赖
```

不要选择“仅上传代码、不安装依赖”，因为函数依赖 `wx-server-sdk`。

### 2. 确认部署成功

进入云开发控制台的“云函数”，应看到四个函数：

```text
user
book
engagement
moderation
```

逐个检查：

- 部署状态正常；
- 运行环境可用；
- `book` 与 `engagement` 已配置相同加密密钥；
- `user`、`book`、`moderation` 的内容安全权限配置已随函数上传；
- 函数日志中没有依赖安装失败。

### 3. 内容安全说明

工程已为以下函数配置文本内容安全权限：

```text
user/config.json
book/config.json
moderation/config.json
```

它们调用 `security.msgSecCheck`。如果后台尚未开通对应开放能力，用户修改昵称、发布或反馈时可能出现“内容安全检查暂不可用”。

当前工程的书籍图片可以上传，但**图片异步内容安全回调和自动复审流程尚未完成**。正式提审前必须二选一：

1. 补齐微信图片内容安全检测与回调处理；或
2. 新发布书籍先进入人工审核状态，通过后再公开。

这不是可忽略的小问题，而是 UGC 小程序的上线阻断项。

---

## 八、第七阶段：切换到 Cloud 模式

打开：

```text
miniprogram/config/env.js
```

改为：

```js
module.exports = {
  enableCloud: true,
  cloudEnvId: '你的开发环境ID',
  pageSize: 6
};
```

注意：这里填的是环境 ID，而不是环境名称。

保存后执行：

1. 清除开发者工具缓存；
2. 重新编译；
3. 打开云函数日志；
4. 从小程序首页开始测试。

### 为什么要清缓存

Mock 模式的数据保存在本地缓存中。如果不清理，可能误以为本地数据已经进入云数据库，或者看到 Mock 收藏状态与云端状态混合。

---

## 九、第八阶段：云端联调

### 1. 单账号测试

使用开发者工具和真机依次验证：

1. 打开“我的”，确认 `users` 集合自动创建用户。
2. 修改头像、昵称、学校。
3. 发布一本书，确认：
   - 图片进入云存储；
   - `books` 有书籍记录；
   - `seller_contacts` 有独立联系方式记录；
   - `books` 文档中没有联系方式明文。
4. 首页能查询到新发布书籍。
5. 搜索能找到该书。
6. 收藏后 `favorites` 有记录。
7. 查看详情后 `history` 有记录。
8. 标记售出后首页不再展示该书。
9. 提交反馈和举报，确认相应集合有记录。

### 2. 双账号测试

必须使用两个不同微信账号：

- 账号 A：发布书籍；
- 账号 B：浏览、收藏、“我想要”、查看联系方式；
- 账号 A：查看自己的发布并标记售出。

重点确认：

- B 能看到 A 的书；
- B 无法编辑或下架 A 的书；
- 公开详情中没有联系方式；
- B 明确确认后才能看到联系方式；
- `contact_views` 中记录了查看行为；
- A 不能通过普通用户入口获得管理员权限。

### 3. 不建议只在云函数控制台点测试

这些云函数会从微信调用上下文读取 OpenID。直接在控制台运行，如果没有模拟微信上下文，可能返回：

```text
UNAUTHORIZED
```

应优先通过小程序页面触发，并结合云函数日志与数据库记录排查。

---

## 十、第九阶段：真机预览

点击开发者工具顶部“预览”，用已添加为体验成员或项目成员的微信扫码。

至少测试：

- iOS 微信；
- Android 微信；
- Wi-Fi 与移动网络；
- 拒绝头像/相册/摄像头相关授权后的提示；
- `chooseAvatar` 和昵称输入；
- 选择图片和拍照；
- ISBN 条码扫码；
- 分享卡片落到正确书籍；
- 复制微信号或手机号；
- 弱网、重复点击、图片上传失败。

开发者工具模拟器不能代替真机测试，尤其是头像昵称、扫码、相册、分享和剪贴板能力。

---

## 十一、第十阶段：小程序后台上线准备

### 1. 服务类目

在小程序后台按当前个人主体可选范围，选择与“校园二手书信息发布与撮合”一致的类目。

不要机械照搬旧教程中的类目名称。类目政策和主体可选范围会变化，以提交时后台显示及审核要求为准。

### 2. 隐私保护指引

按实际功能声明：

- 用户主动填写的昵称与选择的头像；
- 用户选择上传的书籍图片；
- 微信号或手机号；
- 用户发布、反馈与举报内容；
- 为安全审计和运营维护产生的必要操作日志。

隐私说明必须与代码实际行为一致，并提供用户更正、删除和联系开发者的渠道。

### 3. 用户协议和交易边界

页面与审核说明中明确：

```text
平台仅提供信息发布和联系撮合，不提供在线支付、交易担保或物流服务；交易由双方线下完成。
```

不要出现“平台担保”“平台收款”“在线付款”等与实际能力不一致的描述。

### 4. UGC 运营机制

审核前应具备：

- 文本和图片内容安全；
- 举报入口；
- 管理员审核/下架能力；
- 违规用户处置机制；
- 用户账号与个人信息删除流程；
- 审核人员可体验的测试数据。

---

## 十二、第十一阶段：上传、提交审核和发布

### 1. 上传代码

1. 确认当前 `cloudEnvId` 指向生产环境，而不是开发环境。
2. 确认生产环境已部署函数、集合、索引和加密密钥。
3. 在开发者工具中点击“上传”。
4. 填写版本号，例如：

```text
0.1.0
```

5. 填写项目备注，例如：

```text
首版校园二手书信息撮合 MVP，包含发布、搜索、收藏、联系、举报与个人中心。
```

### 2. 提交审核

登录小程序后台，进入“版本管理”，找到刚上传的开发版本并提交审核。

审核说明建议写清：

1. 产品用途：校园二手教材和课外书信息发布与撮合；
2. 不接入微信支付，双方线下交易；
3. 如何发布一本书；
4. 如何举报违规内容；
5. 如何删除账号和个人信息；
6. 如有审核专用账号或操作路径，完整提供。

### 3. 发布

审核通过后，在后台点击“发布”。发布前再核对：

- 生产云环境 ID；
- 云函数版本和环境变量；
- 数据库权限；
- 隐私指引状态；
- 内容安全；
- 线上首页是否有合规种子内容。

---

## 十三、生产环境建议

### 1. 不要直接把开发环境当生产环境

推荐保留：

```text
开发环境：测试代码、测试书籍、测试账号
生产环境：正式版本、真实用户数据
```

每次发布前，将四个函数部署到生产环境，并把 `cloudEnvId` 切到生产环境后重新上传小程序代码。

### 2. 配置监控

至少关注：

- 云函数错误率和超时；
- 数据库调用量；
- 云存储容量和流量；
- 内容安全失败量；
- 发布失败率；
- 联系方式查看频率；
- 举报和反馈待处理数量。

### 3. 数据备份

定期导出关键集合，尤其是：

```text
users
books
seller_contacts
reports
feedback
```

`seller_contacts` 中的数据经过密钥加密；备份数据时也要安全保存对应密钥，但不要放在同一个公开位置。

---

## 十四、常见故障排查

### `cloud init error` 或环境不存在

- 检查 `cloudEnvId` 是否为环境 ID；
- 检查 AppID 与云环境是否属于同一个小程序；
- 新开通环境后等待后台初始化完成再重试。

### 云函数列表没有出现四个函数

- 确认右键的是 `cloudfunctions/user` 等函数目录；
- 选择“上传并部署：云端安装依赖”；
- 检查当前选中的云环境；
- 查看部署日志。

### 发布时提示 `USER_NOT_FOUND`

先打开“我的”或重新进入小程序，让 `user.init` 初始化用户，再发布。

### 发布时提示 `CONTENT_CHECK_FAILED`

- 检查内容安全开放能力和函数权限；
- 确认 `config.json` 已随 `user`、`book`、`moderation` 部署；
- 查看云函数日志中的官方接口错误码。

### 查看联系方式提示配置错误

检查 `book` 和 `engagement` 是否都配置了：

```text
CONTACT_ENCRYPTION_KEY
```

并确认值完全一致。修改环境变量后建议重新部署或重启相关函数实例。

### 首页提示缺少索引

打开错误详情，按微信提供的索引字段和方向在数据库控制台创建索引，等待索引构建完成后重试。

### 云模式发布成功但首页看不到

依次检查：

1. `books.status` 是否为 `published`；
2. `deletedAt` 是否为 `null`；
3. `publishedAt` 是否存在；
4. 当前小程序和云控制台是否选择同一环境；
5. 所需复合索引是否构建完成。

---

## 十五、最终验收清单

### 导入

- [ ] 选择包含 `project.config.json` 的目录
- [ ] Mock 模式编译成功
- [ ] Mock 核心业务链路可运行

### 云开发

- [ ] 已替换正式 AppID
- [ ] 已创建开发和生产环境
- [ ] 已创建 9 个集合
- [ ] 数据库权限为仅云函数可访问
- [ ] 索引已创建
- [ ] `book` 与 `engagement` 使用相同加密密钥
- [ ] 四个云函数部署成功
- [ ] `enableCloud=true` 且环境 ID 正确

### 联调

- [ ] 两个微信账号可完成发布和联系
- [ ] 非发布者无法编辑他人书籍
- [ ] 公开详情不返回联系方式
- [ ] 联系方式查看有审计记录
- [ ] 收藏、足迹、售出、下架正常
- [ ] 文本内容安全正常
- [ ] 图片内容审核方案已落地

### 提审

- [ ] 主体类目与服务一致
- [ ] 隐私保护指引已配置
- [ ] 用户协议和线下交易边界清晰
- [ ] 举报、下架和个人信息删除可演示
- [ ] iOS、Android 真机测试通过
- [ ] 生产环境配置复核完成
